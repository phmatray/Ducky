@if (IsHydrating && LoadingContent != null)
{
    @LoadingContent
}
else if (IsHydrated && ChildContent != null)
{
    @ChildContent
}
else if (!IsHydrating && !IsHydrated && ChildContent != null)
{
    @* Render content before hydration if not auto-hydrating *@
    @ChildContent
}